/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: [
      'www.tuacasa.com.br',
      'projetaronline.com/',
    ]
  }
}

module.exports = nextConfig
